(async function(){
 const templates=window.BERNAUNG_TEMPLATES||[],data=getData(),wrap=document.getElementById('templates'),tabs=document.getElementById('categories');if(!wrap||!tabs)return;const price=await (window.BernaungPriceReady||Promise.resolve(window.BERNAUNG_PRICE));
 function draw(cat='Semua'){const list=cat==='Semua'?templates:templates.filter(t=>t.cat===cat);wrap.innerHTML=list.map(t=>`<article class="template-card"><div class="template-thumb ${t.url?'real-template-thumb':''}">${t.url?`<iframe src="${t.url}" title="Preview ${esc(t.name)}" loading="lazy"></iframe>`:`<div><span>${t.cat.toUpperCase()}</span><strong>${t.name}</strong><span>BERNAUNG</span></div>`}</div><div class="template-info"><b>${esc(t.name)}</b><small>${new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(price||0)}</small></div><button class="btn btn-dark" style="width:calc(100% - 32px);margin:0 16px 16px" onclick="chooseTemplate(${t.id})">Pilih tema</button></article>`).join('')}
 window.chooseTemplate=function(id){const t=templates.find(x=>x.id===id);if(!t)return;const d=getData();d.templateId=t.id;d.templateName=t.name;d.templateCategory=t.cat;d.templateUrl=t.url||'';d.templatePreviewUrl=t.url||'';saveData(d);location.href='/preview'};
 window.filterCat=function(cat,button){document.querySelectorAll('.category-tabs button').forEach(x=>x.classList.remove('active'));button.classList.add('active');draw(cat)};tabs.innerHTML=cats.map((c,i)=>`<button class="${i===0?'active':''}" onclick="filterCat('${c}',this)">${c}</button>`).join('');draw();
})();
