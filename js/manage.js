(function(){
const login=document.getElementById('manageLogin'),panel=document.getElementById('managePanel'),err=document.getElementById('manageError');
if(!login||!panel)return;
const params=new URLSearchParams(location.search);
const manageId=params.get('id')||location.pathname.split('/').filter(Boolean).pop()||'';
let accessCode='',data=null,overview=null;
const templates=[
{id:3,name:'Adat Nusantara',cat:'Elegant',url:'/templates/adat-nusantara.html'},
{id:13,name:'Playful Ceria',cat:'Playful',url:'/templates/playful-ceria.html'}
];
const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const val=v=>String(v??'').trim();
const photoUrls=d=>Array.isArray(d)?d.map(x=>typeof x==='string'?x:(x?.url||'')).map(val).filter(Boolean):[];
const money=n=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(n||0));
function abs(u){if(!u)return'';return /^https?:\/\//i.test(u)?u:location.origin+'/'+String(u).replace(/^\//,'')}
function notice(t,c=''){const n=document.getElementById('manageNotice');if(n){n.textContent=t;n.className='manage-notice '+c}}
function openSavedModal(){const m=document.getElementById('saveSuccessModal');if(m){m.classList.remove('hidden');m.setAttribute('aria-hidden','false')}}
function closeSavedModal(){const m=document.getElementById('saveSuccessModal');if(m){m.classList.add('hidden');m.setAttribute('aria-hidden','true')}}
function scheduleRow(a,i){return `<div class="data-row"><div class="row-fields"><input data-st="title" data-i="${i}" value="${esc(a.title||'')}" placeholder="Nama acara"><input data-st="time" data-i="${i}" value="${esc(a.time||'')}" placeholder="08.00 WIB"></div><button type="button" class="icon-btn danger" data-rs="${i}">Hapus</button></div>`}
function accountCard(a,i){const n=val(a.number),masked=n?('•••• •••• '+n.slice(-4)):'Nomor rekening belum diisi';return `<article class="account-card"><div><span class="mini-label">${esc(a.provider||'REKENING')}</span><strong>${esc(masked)}</strong><small>${esc(a.name||'Nama pemilik belum diisi')}</small></div><div class="row-actions"><button type="button" class="text-btn" data-edit-account="${i}">Edit</button><button type="button" class="text-btn danger-text" data-delete-account="${i}">Hapus</button></div></article>`}
function photoSlot(url,index,label){return `<article class="photo-card" data-photo-card="${index}"><div class="photo-preview ${url?'has-photo':''}">${url?`<img src="${esc(url)}" alt="${esc(label)}">`:'<span>Belum ada foto</span>'}</div><div class="photo-card-info"><div><b>${esc(label)}</b><small>${url?'Foto tersimpan':'Belum diisi'}</small></div><div class="row-actions"><label class="upload-btn">${url?'Ganti foto':'Upload foto'}<input class="photo-file" data-photo="${index}" type="file" accept="image/jpeg,image/png,image/webp"></label>${url?`<button type="button" class="text-btn danger-text" data-remove-photo="${index}">Hapus</button>`:''}</div></div></article>`}
function render(d){
 const ls=Array.isArray(d.loveStory)?d.loveStory:[],ss=Array.isArray(d.schedules)?d.schedules:[],ac=Array.isArray(d.accounts)?d.accounts:[],photos=photoUrls(d.photos||[]);
 const activeTemplate=templates.find(t=>String(t.id)===String(d.templateId))||templates[0];
 const changeCount=Number(d.templateChangeCount||0);
 panel.innerHTML=`
 <div class="manage-workspace-head"><div><span class="eyebrow">BERNAUNG · MANAGE</span><h2>${esc(d.groom||'Mempelai')} <i>&</i> ${esc(d.bride||'')}</h2><p>Kelola, pantau, dan bagikan undangan dari satu ruang.</p></div><div class="manage-head-actions"><a class="btn btn-light btn-sm" href="${esc(d.inviteUrl||('/'+d.invite_slug))}" target="_blank" rel="noopener">Preview</a><button class="btn btn-dark btn-sm" id="saveTop" type="button">Simpan</button></div></div>
 <div class="manage-tabs" role="tablist" aria-label="Manage"><button type="button" class="manage-tab active" data-main-tab="overview">Overview</button><button type="button" class="manage-tab" data-main-tab="edit">Edit Undangan</button><button type="button" class="manage-tab" data-main-tab="guest-links">Link Tamu</button><button type="button" class="manage-tab" data-main-tab="access">Akses</button></div>
 <div class="manage-notice" id="manageNotice">Perubahan disimpan setelah menekan Simpan.</div>
 <div class="manage-tab-panel active" data-tab-panel="overview"><div id="manageOverview" class="manage-overview"></div></div>
 <div class="manage-tab-panel" data-tab-panel="edit">
   <form id="manageEditor" class="manage-editor">
    <section class="manage-section" data-section="identity"><div class="section-head"><div><span class="eyebrow">01</span><h3>Mempelai</h3></div><p>Nama mempelai dan orang tua.</p></div><div class="form-grid"><label>Nama pria<input name="groom" value="${esc(d.groom)}" required></label><label>Nama wanita<input name="bride" value="${esc(d.bride)}" required></label><label>Ayah pria<input name="groomFather" value="${esc(d.groomFather)}"></label><label>Ibu pria<input name="groomMother" value="${esc(d.groomMother)}"></label><label>Ayah wanita<input name="brideFather" value="${esc(d.brideFather)}"></label><label>Ibu wanita<input name="brideMother" value="${esc(d.brideMother)}"></label></div></section>
    <section class="manage-section" data-section="event"><div class="section-head"><div><span class="eyebrow">02</span><h3>Acara</h3></div><p>Hari, waktu, dan lokasi.</p></div><div class="form-grid"><label>Jenis acara<select name="eventType"><option ${d.eventType==='Akad'?'selected':''}>Akad</option><option ${d.eventType==='Resepsi'?'selected':''}>Resepsi</option><option ${d.eventType==='Akad + Resepsi'?'selected':''}>Akad + Resepsi</option></select></label><label>Tanggal<input type="date" name="date" value="${esc(d.date)}" required></label><label>Waktu<input name="time" value="${esc(d.time)}"></label><label>Tempat<input name="venue" value="${esc(d.venue)}"></label><label class="full">Alamat<input name="address" value="${esc(d.address)}"></label><label class="full">Google Maps URL<input name="maps" value="${esc(d.maps)}" required></label></div></section>
    <section class="manage-section" data-section="story"><div class="section-head"><div><span class="eyebrow">03</span><h3>Love Story</h3></div><p>Dua cerita opsional.</p></div><div class="story-grid">${[0,1].map(i=>`<div class="story-card"><span>Cerita ${i+1}</span><input data-ls="title" data-i="${i}" value="${esc(ls[i]?.title||'')}" placeholder="Judul singkat"><textarea data-ls="text" data-i="${i}" rows="5" placeholder="Tulis cerita...">${esc(ls[i]?.text||'')}</textarea></div>`).join('')}</div><p class="helper">Jika kosong, template memakai teks netral bawaan.</p></section>
    <section class="manage-section" data-section="schedule"><div class="section-head"><div><span class="eyebrow">04</span><h3>Susunan Acara</h3></div><p>Jadwal yang tampil di undangan.</p></div><div id="scheduleList" class="stack-list">${ss.map(scheduleRow).join('')}</div><button type="button" class="outline-add" id="addSchedule">+ Tambah acara</button></section>
    <section class="manage-section" data-section="gift"><div class="section-head"><div><span class="eyebrow">05</span><h3>Rekening / Gift</h3></div><p>Informasi hadiah digital.</p></div><div id="accountList" class="account-list">${ac.map(accountCard).join('')||'<div class="empty-inline">Belum ada rekening.</div>'}</div><button type="button" class="outline-add" id="addAccount">+ Tambah rekening</button></section>
    <section class="manage-section" data-section="gallery"><div class="section-head"><div><span class="eyebrow">06</span><h3>Galeri</h3></div><p>Pilih sumber foto untuk undangan.</p></div><div class="photo-source-picker"><label class="photo-source-option"><input type="radio" name="photoSource" value="user" ${d.photoSource==='user'?'checked':''}><span><b>Gunakan foto saya</b><small>Upload 8 foto pribadi.</small></span></label><label class="photo-source-option"><input type="radio" name="photoSource" value="default" ${!d.photoSource||d.photoSource==='default'?'checked':''}><span><b>Default</b><small>Gunakan foto bawaan Bernaung.</small></span></label><label class="photo-source-option"><input type="radio" name="photoSource" value="default-islamic" ${d.photoSource==='default-islamic'?'checked':''}><span><b>Default Islamic</b><small>Gunakan foto bawaan Islamic.</small></span></label></div><div id="customPhotoForm" class="custom-photo-form ${d.photoSource==='user'?'':'hidden'}"><div class="photo-group"><div class="photo-group-head"><div><b>Mempelai pria</b><span>1 foto</span></div><small>Satu slot.</small></div><div class="photo-single">${photoSlot(photos[0]||'',0,'Mempelai pria')}</div></div><div class="photo-group"><div class="photo-group-head"><div><b>Mempelai wanita</b><span>1 foto</span></div><small>Satu slot.</small></div><div class="photo-single">${photoSlot(photos[1]||'',1,'Mempelai wanita')}</div></div><div class="photo-group"><div class="photo-group-head"><div><b>Foto bersama</b><span>6 foto</span></div><small>Enam slot.</small></div><div class="photo-grid">${Array.from({length:6},(_,i)=>photoSlot(photos[i+2]||'',i+2,`Foto bersama ${i+1}`)).join('')}</div></div><p class="helper">JPG, PNG, atau WebP. Maksimal 8 MB per foto.</p></div></div></section>
    <section class="manage-section" data-section="music"><div class="section-head"><div><span class="eyebrow">07</span><h3>Musik</h3></div><p>Musik latar undangan.</p></div><label>URL MP3<input name="music" value="${esc(d.music)}" placeholder="https://domain.com/musik.mp3"></label></section>
    <section class="manage-section" data-section="contact"><div class="section-head"><div><span class="eyebrow">08</span><h3>Kontak</h3></div><p>Nomor WhatsApp untuk tamu.</p></div><label>WhatsApp<input name="whatsapp" value="${esc(d.whatsapp)}" placeholder="628xxxxxxxxxx"></label></section>
    <section class="manage-section" data-section="template"><div class="section-head"><div><span class="eyebrow">09</span><h3>Template</h3></div><p>Ganti tema sesuai kesempatan yang tersedia.</p></div><div class="template-limit"><strong>Penggantian tema: ${changeCount}/${Number(d.templateChangeLimit??2)}</strong><span>${changeCount>=Number(d.templateChangeLimit??2)?'Kesempatan penggantian tema sudah habis.':'Setiap kali memilih tema berbeda, satu kesempatan digunakan.'}</span></div><label>Tema<select name="templateId" id="manageTemplateSelect" ${changeCount>=Number(d.templateChangeLimit??2)?'data-locked="true"':''}>${templates.map(t=>`<option value="${t.id}" ${String(d.templateId)===String(t.id)?'selected':''}>${esc(t.name)} — ${esc(t.cat)}</option>`).join('')}</select></label><div class="template-preview"><iframe id="templateFrame" src="${esc(activeTemplate.url)}" loading="lazy"></iframe><div><span class="mini-label">TEMA AKTIF</span><b id="templateName">${esc(activeTemplate.name)}</b><p id="templateCat">${esc(activeTemplate.cat)}</p></div></div></section>
    <section class="manage-section" data-section="notes"><div class="section-head"><div><span class="eyebrow">10</span><h3>Catatan</h3></div><p>Tambahan untuk undangan.</p></div><textarea name="notes" rows="5" placeholder="Tulis catatan tambahan...">${esc(d.notes||'')}</textarea></section>
    <div class="save-bar"><button class="btn btn-dark" type="submit">Simpan perubahan</button></div>
   </form>
 </div>
 <div class="manage-tab-panel" data-tab-panel="guest-links"><section class="manage-tool-card"><div class="section-head"><div><span class="eyebrow">LINK TAMU</span><h3>Generator link personal</h3></div><p>Satu nama menghasilkan satu link.</p></div><label>Nama tamu <span class="field-note">maksimal 50 nama</span><textarea id="guestNames" rows="10" placeholder="Budi\nSiti\nAndi\nRina"></textarea></label><div class="guest-generator-actions"><button type="button" class="btn btn-dark" id="generateGuestLinks">Generate link</button><span id="guestCount" class="guest-count">0 nama</span></div><div id="guestLinksResult" class="guest-links-result"></div></section></div>
 <div class="manage-tab-panel" data-tab-panel="access"><section class="manage-tool-card access-page-card"><div class="section-head"><div><span class="eyebrow">AKSES</span><h3>Link & kode</h3></div><p>Simpan informasi ini di tempat yang aman.</p></div><div class="access-list"><div class="access-row"><div><span>Link Undangan</span><strong>${esc(abs(d.inviteUrl||('/'+d.invite_slug)))}</strong></div><div class="access-row-actions"><button type="button" class="text-btn" data-copy="${esc(abs(d.inviteUrl||('/'+d.invite_slug)))}">Salin</button><a class="text-btn" href="${esc(abs(d.inviteUrl||('/'+d.invite_slug)))}" target="_blank" rel="noopener">Buka</a></div></div><div class="access-row"><div><span>Link Manage</span><strong>${esc(abs(d.manageUrl||('/m/'+manageId)))}</strong></div><div class="access-row-actions"><button type="button" class="text-btn" data-copy="${esc(abs(d.manageUrl||('/m/'+manageId)))}">Salin</button><a class="text-btn" href="${esc(abs(d.manageUrl||('/m/'+manageId)))}" target="_blank" rel="noopener">Buka</a></div></div><div class="access-row"><div><span>Kode Rahasia</span><strong id="secretCodeValue">••••••••</strong></div><div class="access-row-actions"><button type="button" class="text-btn" id="toggleSecret">Tampilkan</button><button type="button" class="text-btn" data-copy="${esc(accessCode)}">Salin</button></div></div></div><a class="btn btn-dark full-btn" href="https://wa.me/?text=${encodeURIComponent('Undangan '+(d.groom||'')+' & '+(d.bride||'')+' — '+abs(d.inviteUrl||('/'+d.invite_slug)))}" target="_blank" rel="noopener">Bagikan via WhatsApp</a></section></div>
 <div id="accountModal" class="modal hidden"><div class="modal-backdrop" data-close-modal></div><div class="modal-card"><div class="modal-head"><div><span class="eyebrow">REKENING / GIFT</span><h3 id="accountModalTitle">Tambah rekening</h3></div><button type="button" class="modal-close" data-close-modal>×</button></div><form id="accountForm"><label>Bank / e-wallet<input name="provider" required placeholder="BCA / Mandiri / DANA"></label><label>Nomor rekening<input name="number" required inputmode="numeric"></label><label>Nama pemilik<input name="name" required></label><div class="modal-actions"><button type="button" class="btn btn-light" data-close-modal>Batal</button><button type="submit" class="btn btn-dark">Simpan</button></div></form></div></div>
 <div id="saveSuccessModal" class="success-modal hidden" aria-hidden="true"><div class="success-backdrop" data-close-success></div><div class="success-card" role="dialog" aria-modal="true"><div class="success-mark">✓</div><span class="eyebrow">TERSIMPAN</span><h3>Perubahan berhasil disimpan</h3><p>Semua perubahan pada undanganmu sudah tersimpan.</p><button type="button" class="btn btn-dark full-btn" data-close-success>Oke</button></div></div>
 <div id="confirmModal" class="confirm-modal hidden" aria-hidden="true"><div class="confirm-backdrop" data-close-confirm></div><div class="confirm-card" role="dialog" aria-modal="true"><div class="confirm-icon">!</div><div><span class="eyebrow">KONFIRMASI</span><h3 id="confirmTitle">Hapus item ini?</h3><p id="confirmText">Tindakan ini tidak dapat dibatalkan.</p></div><div class="confirm-actions"><button type="button" class="btn btn-light" data-close-confirm>Batal</button><button type="button" class="btn btn-danger-solid" id="confirmAction">Hapus</button></div></div></div>`;
 let loveStory=ls.slice(0,2);while(loveStory.length<2)loveStory.push({title:'',text:''});
 let schedules=ss.slice(),accounts=ac.slice(),confirmCallback=null;
 function setTab(name){document.querySelectorAll('.manage-tab').forEach(b=>b.classList.toggle('active',b.dataset.mainTab===name));document.querySelectorAll('.manage-tab-panel').forEach(p=>p.classList.toggle('active',p.dataset.tabPanel===name));if(name==='overview')loadOverview();}
 function rerenderSchedules(){document.getElementById('scheduleList').innerHTML=schedules.map(scheduleRow).join('')||'<div class="empty-inline">Belum ada susunan acara.</div>'}
 function rerenderAccounts(){document.getElementById('accountList').innerHTML=accounts.map(accountCard).join('')||'<div class="empty-inline">Belum ada rekening.</div>'}
 function openAccount(i=null){const m=document.getElementById('accountModal'),f=document.getElementById('accountForm');f.reset();f.dataset.edit=i===null?'':String(i);document.getElementById('accountModalTitle').textContent=i===null?'Tambah rekening':'Edit rekening';if(i!==null&&accounts[i]){f.provider.value=accounts[i].provider||'';f.number.value=accounts[i].number||'';f.name.value=accounts[i].name||''}m.classList.remove('hidden')}
 function closeAccount(){document.getElementById('accountModal')?.classList.add('hidden')}
 function openConfirm(title,text,cb){document.getElementById('confirmTitle').textContent=title;document.getElementById('confirmText').textContent=text;confirmCallback=cb;document.getElementById('confirmModal').classList.remove('hidden')}
 function closeConfirm(){document.getElementById('confirmModal').classList.add('hidden');confirmCallback=null}
 function reminderDue(date, enabled, days){if(!enabled||!date)return false;const d=new Date(date+'T00:00:00');if(Number.isNaN(d.getTime()))return false;d.setDate(d.getDate()+Number(days||14));const today=new Date();today.setHours(0,0,0,0);return today>=d}
 async function loadOverview(){const box=document.getElementById('manageOverview');if(!box)return;box.innerHTML='<div class="overview-loading">Memuat ringkasan…</div>';if(!supabaseReady()){box.innerHTML='<div class="empty-inline">Overview tersedia setelah Supabase aktif.</div>';return}try{const {data:r,error}=await supabaseClient.rpc('get_manage_overview',{p_manage_id:manageId,p_secret_code:accessCode});if(error)throw error;overview=r||{};const stats=overview.stats||{};const rsvps=Array.isArray(overview.rsvps)?overview.rsvps:[],greetings=Array.isArray(overview.greetings)?overview.greetings:[];const hadir=rsvps.filter(x=>x.attendance==='hadir'),tidak=rsvps.filter(x=>x.attendance==='tidak_hadir');const reminder=reminderDue(data.date,data.reminderEnabled!==false,data.reminderDays||14)?'<div class="manage-reminder"><b>Waktunya meninjau undangan.</b><span>Acara sudah lewat '+Number(data.reminderDays||14)+' hari. Tinjau undanganmu dan pilih apakah ingin tetap menyimpannya atau menutupnya.</span></div>':'';box.innerHTML=reminder+`<div class="overview-stats"><div><span>Undangan dibuka</span><strong>${Number(stats.totalOpens||0)}</strong></div><div><span>Pengunjung unik</span><strong>${Number(stats.uniqueVisitors||0)}</strong></div><div><span>Hadir</span><strong>${hadir.length}</strong></div><div><span>Tidak hadir</span><strong>${tidak.length}</strong></div><div><span>Belum RSVP</span><strong>${Math.max(0,Number(stats.uniqueRsvpGuests||0)-hadir.length-tidak.length)}</strong></div><div><span>Terakhir dibuka</span><strong>${stats.lastOpened?new Date(stats.lastOpened).toLocaleString('id-ID',{dateStyle:'medium',timeStyle:'short'}):'Belum ada'}</strong></div></div><div class="overview-grid"><section class="overview-card"><div class="overview-card-head"><div><span class="eyebrow">RSVP</span><h3>Yang hadir</h3></div><span>${hadir.reduce((n,x)=>n+Number(x.guest_count||1),0)} orang</span></div>${hadir.length?hadir.map(x=>`<article class="overview-person"><div><strong>${esc(x.guest_name)}</strong><span>${esc(x.guest_count)} orang</span></div><p>${esc(x.message||'Tidak ada ucapan.')}</p><small>${new Date(x.created_at).toLocaleString('id-ID',{dateStyle:'medium',timeStyle:'short'})}</small></article>`).join(''):'<div class="empty-inline">Belum ada RSVP hadir.</div>'}</section><section class="overview-card"><div class="overview-card-head"><div><span class="eyebrow">RSVP</span><h3>Tidak hadir</h3></div><span>${tidak.reduce((n,x)=>n+Number(x.guest_count||1),0)} orang</span></div>${tidak.length?tidak.map(x=>`<article class="overview-person"><div><strong>${esc(x.guest_name)}</strong><span>${esc(x.guest_count)} orang</span></div><p>${esc(x.message||'Tidak ada ucapan.')}</p><small>${new Date(x.created_at).toLocaleString('id-ID',{dateStyle:'medium',timeStyle:'short'})}</small></article>`).join(''):'<div class="empty-inline">Belum ada RSVP tidak hadir.</div>'}</section></div><section class="overview-card"><div class="overview-card-head"><div><span class="eyebrow">UCAPAN</span><h3>Ucapan terbaru</h3></div><span>${greetings.length} ucapan</span></div>${greetings.length?greetings.slice(0,10).map(x=>`<article class="overview-person"><div><strong>${esc(x.guest_name)}</strong></div><p>${esc(x.message)}</p><small>${new Date(x.created_at).toLocaleString('id-ID',{dateStyle:'medium',timeStyle:'short'})}</small></article>`).join(''):'<div class="empty-inline">Belum ada ucapan.</div>'}</section><section class="overview-event"><div><span class="eyebrow">ACARA</span><h3>${esc(data.groom||'-')} &amp; ${esc(data.bride||'-')}</h3><p>${esc(data.eventType||'-')} · ${esc(data.date||'-')} · ${esc(data.venue||'-')}</p></div><div><span class="eyebrow">TEMA</span><strong>${esc(data.templateName||'-')}</strong></div></section>`}catch(x){console.error(x);box.innerHTML='<div class="empty-inline">Gagal memuat Overview.</div>'}}
 function guestLinkItems(){const raw=String(document.getElementById('guestNames').value||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean).slice(0,50);document.getElementById('guestCount').textContent=`${raw.length} nama`;return raw}
 function generateGuestLinks(){const names=guestLinkItems(),base=abs(data.inviteUrl||('/'+data.invite_slug));const box=document.getElementById('guestLinksResult');if(!names.length){box.innerHTML='<div class="empty-inline">Masukkan nama tamu terlebih dahulu.</div>';return}box.innerHTML=`<div class="guest-result-actions"><button type="button" class="btn btn-light" id="copyAllGuests">Salin semua</button><button type="button" class="btn btn-light" id="exportGuests">Export TXT</button></div>`+names.map((name,i)=>{const url=base+'?to='+encodeURIComponent(name);return `<div class="guest-link-row"><div><strong>${esc(name)}</strong><span>${esc(url)}</span></div><button type="button" class="text-btn" data-copy="${esc(url)}">Salin</button></div>`}).join('');const all=names.map(n=>`${n}\n${base}?to=${encodeURIComponent(n)}`).join('\n\n');document.getElementById('copyAllGuests').onclick=async()=>{try{await navigator.clipboard.writeText(names.map(n=>base+'?to='+encodeURIComponent(n)).join('\n'));notice('Semua link tamu disalin.','success')}catch(_){notice('Gagal menyalin.','error')}};document.getElementById('exportGuests').onclick=()=>{const blob=new Blob([all+'\n'],{type:'text/plain;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`link-tamu-${String(data.invite_slug||'undangan')}.txt`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}}
 function collect(){const fd=new FormData(document.getElementById('manageEditor')),t=templates.find(x=>String(x.id)===String(fd.get('templateId')))||templates[0];return {groom:val(fd.get('groom')),bride:val(fd.get('bride')),groomFather:val(fd.get('groomFather')),groomMother:val(fd.get('groomMother')),brideFather:val(fd.get('brideFather')),brideMother:val(fd.get('brideMother')),eventType:val(fd.get('eventType')),date:val(fd.get('date')),time:val(fd.get('time')),venue:val(fd.get('venue')),address:val(fd.get('address')),maps:val(fd.get('maps')),whatsapp:val(fd.get('whatsapp')),music:val(fd.get('music')),notes:val(fd.get('notes')),photos:photoUrls(data.photos||[]),photoSource:val(fd.get('photoSource'))||'default',loveStory:loveStory.filter(x=>x?.title||x?.text),schedules:schedules.filter(x=>x.title||x.time),accounts:accounts.filter(x=>x.provider||x.number||x.name),templateId:t.id,templateName:t.name,templateCategory:t.cat,templateUrl:t.url}}
 async function save(e){e?.preventDefault();const next=collect();if(!next.groom||!next.bride||!next.date||!next.maps){notice('Nama, tanggal, dan Google Maps wajib diisi.','error');return}const oldTemplate=String(data.templateId||'');if(Number(data.templateChangeCount||0)>=Number(data.templateChangeLimit??2)&&String(next.templateId)!==oldTemplate){notice('Kesempatan penggantian tema sudah habis.','error');document.getElementById('manageTemplateSelect').value=oldTemplate;return}const buttons=[document.getElementById('saveTop'),document.querySelector('.save-bar button')].filter(Boolean);buttons.forEach(b=>{b.disabled=true;b.dataset.originalText=b.textContent;b.textContent='Menyimpan…'});try{if(!supabaseReady()){data={...data,...next};saveData({...getData(),...next});notice('Perubahan berhasil disimpan.','success');openSavedModal();return}const {data:r,error}=await supabaseClient.rpc('update_manage_invitation',{p_manage_id:manageId,p_secret_code:accessCode,p_data:next});if(error)throw error;data={...data,...next,inviteUrl:r?.inviteUrl||data.inviteUrl,manageUrl:r?.manageUrl||data.manageUrl,templateChangeCount:Number(r?.templateChangeCount??data.templateChangeCount??0),templateChangeLimit:Number(r?.templateChangeLimit??data.templateChangeLimit??2)};notice('Perubahan berhasil disimpan.','success');openSavedModal();if(String(next.templateId)!==oldTemplate){render(data);setTab('edit')}}catch(x){console.error(x);notice(x.message||'Gagal menyimpan.','error')}finally{buttons.forEach(b=>{b.disabled=false;b.textContent=b.dataset.originalText||'Simpan'})}}
 document.querySelectorAll('[data-main-tab]').forEach(b=>b.onclick=()=>setTab(b.dataset.mainTab));
 document.getElementById('manageEditor').addEventListener('input',e=>{const i=Number(e.target.dataset.i);if(e.target.dataset.st)schedules[i][e.target.dataset.st]=e.target.value;if(e.target.dataset.ls)loveStory[i][e.target.dataset.ls]=e.target.value});
 document.getElementById('addSchedule').onclick=()=>{schedules.push({title:'',time:''});rerenderSchedules()};document.getElementById('addAccount').onclick=()=>openAccount();
 panel.addEventListener('click',async e=>{const cp=e.target.closest('[data-copy]');if(cp){try{await navigator.clipboard.writeText(cp.dataset.copy||'');notice('Disalin.','success')}catch(_){notice('Gagal menyalin.','error')}}const b=e.target.closest('[data-remove-photo]');if(b){const i=Number(b.dataset.removePhoto);openConfirm('Hapus foto ini?','Foto akan dihapus dari galeri undangan.',async()=>{const next=photoUrls(data.photos||[]);next[i]='';while(next.length&&!next[next.length-1])next.pop();try{await persistPhotos(next);data.photos=next;render(data);setTab('edit');notice('Foto dihapus.','success')}catch(x){notice(x.message||'Gagal menghapus foto.','error')}})}if(e.target.closest('[data-edit-account]'))openAccount(Number(e.target.closest('[data-edit-account]').dataset.editAccount));if(e.target.closest('[data-delete-account]')){const i=Number(e.target.closest('[data-delete-account]').dataset.deleteAccount);openConfirm('Hapus rekening ini?','Data rekening ini akan dihapus dari undangan.',()=>{accounts.splice(i,1);rerenderAccounts()})}if(e.target.closest('[data-close-success]'))closeSavedModal();if(e.target.closest('[data-close-confirm]'))closeConfirm();if(e.target.closest('[data-close-modal]'))closeAccount();});
 document.getElementById('manageEditor').addEventListener('change',e=>{const r=e.target.closest('input[name=photoSource]');if(!r)return;const box=document.getElementById('customPhotoForm');box?.classList.toggle('hidden',r.value!=='user')});
document.getElementById('accountForm').addEventListener('submit',e=>{e.preventDefault();const f=e.currentTarget,i=f.dataset.edit===''?null:Number(f.dataset.edit),item={provider:val(f.provider.value),number:val(f.number.value),name:val(f.name.value)};if(i===null)accounts.push(item);else accounts[i]=item;rerenderAccounts();closeAccount()});
 document.getElementById('confirmAction').onclick=async()=>{const cb=confirmCallback;closeConfirm();if(cb)await cb()};
 document.getElementById('manageTemplateSelect').addEventListener('change',e=>{const current=String(data.templateId||'');if(Number(data.templateChangeCount||0)>=Number(data.templateChangeLimit??2)&&e.target.value!==current){e.target.value=current;notice('Kesempatan penggantian tema sudah habis.','error');return}const t=templates.find(x=>String(x.id)===String(e.target.value))||templates[0];document.getElementById('templateFrame').src=t.url;document.getElementById('templateName').textContent=t.name;document.getElementById('templateCat').textContent=t.cat});
 document.getElementById('generateGuestLinks').onclick=generateGuestLinks;document.getElementById('guestNames').addEventListener('input',guestLinkItems);
 document.getElementById('toggleSecret').onclick=e=>{const s=document.getElementById('secretCodeValue');const shown=s.textContent===accessCode;s.textContent=shown?'••••••••':accessCode;e.currentTarget.textContent=shown?'Tampilkan':'Sembunyikan'};
 document.getElementById('manageEditor').addEventListener('submit',save);document.getElementById('saveTop').onclick=save;
 document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeConfirm();closeAccount();closeSavedModal()}});
 async function uploadPhoto(index,file){if(!file)return;if(file.size>8*1024*1024){notice('Ukuran foto maksimal 8 MB.','error');return}if(!supabaseReady()){notice('Upload foto membutuhkan Supabase aktif.','error');return}const ext=(file.name.split('.').pop()||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'')||'jpg';const path=`${manageId}/${Date.now()}-${Math.random().toString(36).slice(2,9)}.${ext}`;notice('Mengunggah foto…');try{const {error}=await supabaseClient.storage.from('invitation-photos').upload(path,file,{cacheControl:'3600',upsert:false,contentType:file.type});if(error)throw error;const {data:pub}=supabaseClient.storage.from('invitation-photos').getPublicUrl(path);const next=photoUrls(data.photos||[]);next[index]=pub.publicUrl;data.photos=next;await persistPhotos(next);render(data);setTab('edit');notice('Foto berhasil diunggah.','success')}catch(x){console.error(x);notice(x.message||'Gagal mengunggah foto.','error')}}
 async function persistPhotos(next){const {error}=await supabaseClient.rpc('update_manage_invitation',{p_manage_id:manageId,p_secret_code:accessCode,p_data:{photos:next}});if(error)throw error}
 panel.addEventListener('change',e=>{const input=e.target.closest('.photo-file');if(input&&input.files[0])uploadPhoto(Number(input.dataset.photo),input.files[0])});
 }
 async function loadRsvpLegacy(){return null}
 login.addEventListener('submit',async e=>{e.preventDefault();accessCode=val(new FormData(login).get('code')).toUpperCase();err.textContent='';err.classList.remove('show');try{if(!manageId)throw Error('Link Manage tidak valid.');if(!supabaseReady()){data=getData();if(accessCode!==String(data.secretCode||data.secret||'').toUpperCase())throw Error('Kode rahasia tidak sesuai.')}else{const {data:r,error}=await supabaseClient.rpc('get_manage_invitation',{p_manage_id:manageId,p_secret_code:accessCode});if(error)throw error;if(!r)throw Error('Kode rahasia tidak sesuai atau undangan tidak aktif.');data=r}login.classList.add('hidden');panel.classList.remove('hidden');render(data);loadOverview()}catch(x){err.textContent=x.message||'Kode rahasia tidak sesuai.';err.classList.add('show')}});
})();
