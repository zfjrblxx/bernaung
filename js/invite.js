const d=getData();if(d.groom)document.getElementById('groomName').textContent=d.groom;if(d.bride)document.getElementById('brideName').textContent=d.bride;document.getElementById('couple').textContent=`${d.groom||'Fajar'} & ${d.bride||'Aulia'}`;if(d.date){const dt=new Date(d.date+'T00:00:00');const s=dt.toLocaleDateString('id-ID',{day:'numeric',month:'long',year:'numeric'});document.getElementById('date').textContent=s;document.getElementById('eventDate').textContent=s}if(d.time)document.querySelector('.event-card span').textContent=d.time;if(d.venue)document.getElementById('venue').textContent=d.venue;if(d.address)document.getElementById('address').textContent=d.address;if(d.maps)document.getElementById('mapsLink').href=d.maps;const params=new URLSearchParams(location.search);const to=params.get('to');if(to){document.querySelector('.invite-hero p').textContent=`Kepada Yth. ${to}`};document.getElementById('openInviteBtn').addEventListener('click',()=>document.querySelector('.invite-section').scrollIntoView({behavior:'smooth'}));